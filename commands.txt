commands:
python -m venv venv
.\venv\Scripts\activate

packages:
pip install flask
pip install -U Flask-SQLAlchemy
pip install -U sqlalchemy
pip install pandas Faker mysqlclient

pip install flask_serialize
pip install pymongo[srv]

pip freeze > requirements.txt